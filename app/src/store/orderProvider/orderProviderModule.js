import {mutations} from './orderProviderMutation'
import {getters} from './orderProviderGetters'

const state = {
    items:['ss']
}


export default {
    state:{
        items:['ff']
    },
    mutations:{
    },
}